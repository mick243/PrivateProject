node_modules/
.next/
# bench 가 A/B 비교용으로 뽑아 두는 빌드 산출물 (각 99MB)
.next-ab/
.next-before/
.pglite/
# 첨부 원본 (사진 · 동영상). 이 저장소는 **공개** 라 추적하지 않습니다 — 사용자가
# 올린 파일이 그대로 공개되고, 한 번 커밋하면 이력에서 지우는 데 force push 가 듭니다.
# 그래서 clone · 워크트리에는 첨부가 없고 글의 사진·영상이 404 입니다. 의도된 동작이고,
# 원본은 주 체크아웃의 uploads/ 에 있습니다 (lib/uploads.ts 가 process.cwd() 기준으로 읽음).
# 백업은 git 이 아니라 tar.zst 로 뜹니다 — deploy/README.md 의 복구 절차를 보세요.
uploads/
# DB 덤프 (pg_dump). 파괴적 작업 전에 떠 두는 복구용 — 커밋하면 안 됩니다
backups/
.env.local
next-env.d.ts
*.tsbuildinfo
coverage/
# 워크트리 (git worktree add 가 만드는 별도 작업 트리)
.claude/worktrees/

# 오락실 수입 스크립트의 진행 상태 (하루 한도·이어서 하기)
.arcade-import-state.json

# npm run games:localdata 가 뽑아낸 인허가 목록 (원본은 공공데이터포털에 있음)
localdata/

# 매일 도는 소식 동기화가 남기는 기록 (scripts/sync-piu-news.cmd)
logs/
